# NUTRIENT — philosophy as truthful global address encounter

status: OPEN / UNRESOLVED
source: explicit user-designed Philosophy interaction cleanup
date: 2026-09-19
target: github.cambium...display.philosophy
owner: Philosophy site-holon, with bounded Display consequence only where shared membrane UI becomes dead

## requested encounter
1. The four rank-1 tetrahedral cells forming Philosophy's background should display the globally existing site addresses at the geometric addresses where those site-holons are actually mounted. This must derive from Display Population/mount truth, not Philosophy-authored duplicate occupancy.
2. When the witness focuses/zooms to an address such as Inquiry and the mounted occupant label `[papers]` is encountered there, clicking that occupant should navigate directly to that site-holon. The existing lower-left `ENTER xyz` / commit step is therefore redundant if no other live interaction needs it.
3. Philosophy address navigation should be face-oriented rather than vertex-oriented: clicking the visible face/cell surface should select the corresponding recursive address and frame that address at its centroid, matching the existing centroid camera-focus law.

## boundary
- Philosophy does not become authority for global site placement; Display Population tree/registry remains mount truth.
- Site identity and address remain distinct; labels represent mounted interlocutors at their actual raw global addresses.
- Direct occupant navigation crosses through the existing Display global-navigation event/membrane, not by URL hacks.
- Face hit-testing must preserve recursive address geometry and must not redefine semantic loci, quotient identity or site-local roots.
- Remove the global commit/ENTER membrane only if the new direct Philosophy interaction leaves it with no remaining live function.

## exit
In the generated artifact, Philosophy visibly marks current mounted global site-holons at their true recursive addresses; face clicks focus the corresponding recursive cell/centroid; clicking a mounted occupant marker directly performs the existing target-origin global encounter transition; no redundant ENTER/commit control remains if unused. Structural/address tests and real-browser witnesses pass before Philosophy HOME, with a separate bounded Display HOME only if Display-owned membrane tissue changed.
