# NUTRIENT — event-driven Papers public-shadow circulation

status: OPEN / UNRESOLVED
source: explicit user discussion; current Papers receptor already defines the intended source→shadow membrane
admitted_at: 2026-09-18
target: github.cambium...display.papers
owner: public Papers site-holon / its private-to-public projection membrane

## witnessed difference
Drive `/papers` is the semantic/source authority and its local `_feed` advances after Papers HOME. The public Papers body in GitHub/page is a bounded static shadow of that source-owned feed. The intended relation is already earned as:

`Drive Papers HOME/feed change → authenticated write actuator / Worker → mirrored static shadow in GitHub/public artifact → visitor read`

The event-driven write path is not yet actually closed end-to-end, so the public shadow can remain behind the current Drive Papers feed. This same missing circulation also blocks newly available public-safe Source/Holon inquiry tissue from reaching the page automatically.

## unresolved pressure
Close and witness the event-driven transport so a relevant Drive `/papers/_feed` change causes the Worker/write actuator to refresh only the bounded public-safe Papers projection in GitHub/static publication, after which the normal public build exposes that exact source revision.

## boundary
- visitor/browser reads remain static/cacheable and never actuate the authenticated write path;
- Drive `/papers` remains source authority; GitHub/public projection never becomes a second research database;
- preserve event/source revision identity and idempotence so retries do not duplicate or regress projection state;
- publish only tissue admitted through the Papers public membrane; never leak private Drive IDs/URLs, acquisition tissue, credentials or private nested-organ interiors;
- transport does not invent Papers semantics or renderer content that the source feed did not expose.

## exit condition
A controlled relevant Papers HOME/feed advance causally produces one authenticated Worker/write event, one bounded GitHub/static-shadow refresh and a successfully built public artifact that can be traced to the same source revision; a retry is idempotent, no visitor request is required to trigger the write, and no private source identifiers cross the public membrane.
