# OBSERVATION — source inquiry hydration gate

status: OPEN / UNRESOLVED
source: isolated Papers observation pass, exact staging artifact + current Papers receptor + renderer
date: 2026-09-18
target: display.papers

## observation
Papers RITUAL v1.2 says a selected `S.*` may reveal admitted internal source-body `4V / 6E / 4F / 1T` and metabolites through the same inquiry grammar used by Holons.

The current renderer does not route Source selection through inquiry hydration:
- `detailPanel()` chooses `sourceDetail()` for `S.*`;
- `paintSelection()` calls `hydrateInquiry(...)` only when `!selectedId.startsWith('S.')`;
- the compiled `INQUIRY` object currently contains no `S.*` keys.

In the exact staging artifact, selecting `S.boAF` therefore shows attribution, external links, closure metadata, and descendants, but no internal Source inquiry pore/body.

## evidence boundary
This establishes a local renderer/contract mismatch. It does not establish that any particular Source internal body is currently safe or authorized for public secretion.

## pressure
If admitted Source inquiry tissue arrives later, the current selection path still cannot display it. Decide how Source inquiry should hydrate without fabricating unavailable body content or leaking private source tissue.

## exit condition
A selected `S.*` with admitted public inquiry tissue can reveal its internal `4V / 6E / 4F / 1T` and metabolites; a Source without admitted body tissue exposes explicit missingness rather than silently omitting the inquiry function; provenance remains outward and private identifiers remain sealed.
