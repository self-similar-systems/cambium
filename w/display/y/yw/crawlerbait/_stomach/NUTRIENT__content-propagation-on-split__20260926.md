# NUTRIENT — content propagation on split — 2026-09-26

status: OPEN / REGROWTH PRESSURE
kind: crawlerbait structural encounter
source: display container→content descent metabolism (display HOME display-container-content-descent-20260926)
target: github.cambium → display → crawlerbait (w · Baits)

## source-faithful pressure

Philipp: Crawlerbait uses infinite recursion "wrong". Under the Cambium wood law, when a vertex
splits its entire content propagates to the children; a Sierpinski self-child is the same
geometric point (`ua^m ~ ua`, `xx` = `x`). Program organs keep their adjusted split law (the
parent becomes a clean router for historic incoming dependencies); Crawlerbait's bait-space is
not a program.

Observed: Crawlerbait's bait tree treats split prefixes as routers and keeps bait tissue only at
leaf addresses; content does not propagate on split.

## consequence already visible

Display Descent now renders two levels and pools deeper content, so Crawlerbait is walkable.
Its own bait-space law must decide how baits propagate on differentiation; Display does not
regrow Crawlerbait's interior.

## open

Which occupant stays at the self-child when a collision differentiates an address, and how is
that expressed in `w/w<a>/bait.json` without changing bait identity (observed path)?
